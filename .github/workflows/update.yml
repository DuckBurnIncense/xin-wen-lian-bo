name: Update today xin wen lian bo

on:
  workflow_dispatch:
  schedule:
    - cron: '00 23 * * *'

# permissions:
#   contents: read

jobs:
  update:
    name: Update today xin wen lian bo
    runs-on: ubuntu-latest
    steps:
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          cache: npm
      - name: npm i
        run: npm i
      - name: npm run index
        run: npm run index
